# Visual assets

Binary image files are intentionally not committed because the branch update system rejects binary files.

- Current prototype visuals are CSS gradients, map layers, and text character badges.
- Keep this directory text-only until the delivery path supports binary assets.
- When binary assets are supported, use PNG as the default replacement format for UI art, characters, area thumbnails, and demo screenshots.
