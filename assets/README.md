# Visual assets

All replaceable game artwork is collected here.

- `ui/`: shared interface/background art.
- `characters/`: character portraits/icons.
- `areas/`: exploration destination thumbnails.

The current assets are lightweight SVG placeholders designed for the prototype UI. Replace files in-place to reskin the game without touching JavaScript.
